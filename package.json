{
  "name": "tlou-countdown",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite --host=192.168.50.105",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "tsc && vite build && aws s3 sync ./dist s3://endureandsurvive.io"
  },
  "dependencies": {
    "preact": "^10.5.15"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.1.5",
    "typescript": "^4.5.4",
    "vite": "^2.9.9"
  }
}